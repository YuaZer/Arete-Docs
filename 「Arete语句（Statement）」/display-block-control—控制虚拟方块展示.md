# 🧊 display-block-control — 控制虚拟方块展示

> [🏠 首页](/) | [📚 语句总览](index.md) | **展示实体** | [← 🧱 display-block](display-block.md) | [→ 🎁 display-item](display-item.md)

---

## 📖 简介

`display-block-control` 用于**操控已创建的 BlockDisplay**，
通过位移、缩放、旋转及动画，让虚拟方块展示动起来。目标实体应来自 `display-block` 语句并存储在 `ctx.vars` 中。

---

## ⚙️ 参数说明

| 参数名                  | 类型           | 默认值    | 说明                                    |
|----------------------|--------------|--------|---------------------------------------|
| `target`             | String       | _(必填)_ | `ctx.vars` 中保存 BlockDisplay 的键名。      |
| `set-translation`    | String       | _(可选)_ | 绝对平移到 `x,y,z`。                        |
| `add-translation`    | String       | _(可选)_ | 在原有基础上相对平移 `dx,dy,dz`。                |
| `set-scale`          | String       | _(可选)_ | 将缩放设置为 `sx,sy,sz`。                    |
| `uniform-scale`      | Float/String | _(可选)_ | 统一缩放为 `s`。                            |
| `set-rotation`       | String       | _(可选)_ | 绝对欧拉角旋转 `yaw,pitch,roll`（度）。          |
| `add-rotation`       | String       | _(可选)_ | 相对欧拉角旋转 `yaw,pitch,roll`。             |
| `anim-move`          | String       | _(可选)_ | 以动画移动到 `x,y,z`。                       |
| `anim-move-duration` | Long         | _(可选)_ | 移动动画的持续 tick 数，需与 `anim-move` 搭配。     |
| `anim-spin`          | String       | _(可选)_ | 每 tick 旋转量 `yawPer,pitchPer,rollPer`。 |
| `anim-spin-duration` | Long         | `0`    | 自转动画持续 tick 数，缺省或 `<=0` 为无限循环。        |

> ℹ️ 所有向量参数均接受 `,` / 空格 / `;` 分隔，至少提供三个数值。

---

## 🧠 常见用法

### ✅ 连续平移
```yaml
display-block-control {
  target = "cube"
  add-translation = "0,1,0"
}
```

### ✅ 设置统一缩放并旋转
```yaml
display-block-control {
  target = "cube"
  uniform-scale = "1.2"
  add-rotation = "0,10,0"
}
```

### ✅ 制作位移动画
```yaml
display-block-control {
  target = "cube"
  anim-move = "0,5,0"
  anim-move-duration = "40"
}
```

### ✅ 制作持续自转
```yaml
display-block-control {
  target = "cube"
  anim-spin = "0,5,0"
  # anim-spin-duration 省略 => 无限循环
}
```

---

## 💡 使用建议

- **指令组合**：可多次调用该语句，实现分段动画（例如移动 → 停顿 → 自转）。
- **与 `delay` 配合**：搭配延迟语句控制动画节奏。
- **变量约定**：推荐使用统一前缀（如 `display_`）存储变量，便于管理。
- **安全判断**：若目标不存在，语句会静默返回，可先用 `if` 或 `kether` 校验。

---

## ⚠️ 注意事项

- 动画依赖服务器主线程调度，请确保插件主线程不会被阻塞。
- `anim-move` 必须搭配 `anim-move-duration`，否则不会触发。
- 无限自转会持续占用调度任务，结束时请手动再调用一次控制语句关闭或移除实体。

---

## 🔗 相关语句

- [`display-block`](display-block—创建虚拟方块展示.md) — 创建 BlockDisplay
- [`display-item`](display-item—创建虚拟物品展示.md) — 创建 ItemDisplay
- [`display-item-control`](display-item-control—控制虚拟物品展示.md) — 控制 ItemDisplay