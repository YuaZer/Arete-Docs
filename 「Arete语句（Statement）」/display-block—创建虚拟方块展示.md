# 🧱 display-block — 创建虚拟方块展示

> [🏠 首页](/) | [📚 语句总览](index.md) | **展示实体** | [→ 🧊 display-block-control](display-block-control.md)

---

## 📖 简介

`display-block` 语句会在世界中生成一个 **BlockDisplay 虚拟方块**，
不会修改真实方块而是通过展示实体渲染，同时将该实体引用保存在 `ctx.vars[var]` 供后续语句使用。

适用于自定义模型、悬浮装饰、技能提示等需要精准控制渲染的场景，可与控制语句搭配实现动画。

---

## ⚙️ 参数说明

| 参数名              | 类型      | 默认值       | 说明                                                      |
|------------------|---------|-----------|---------------------------------------------------------|
| `var`            | String  | _(必填)_    | 保存到上下文变量的名称，后续可通过 `${vars.xxx}` 或控制语句引用。                |
| `at`             | String  | `"@self"` | 生成位置解析表达式，支持玩家、实体或坐标。                                   |
| `material`       | String  | `"STONE"` | 方块类型，遵循 Bukkit `Material` 枚举。与 `blockdata` 同时存在时被忽略。    |
| `blockdata`      | String  | _(可选)_    | 直接指定完整 BlockData 字符串，优先于 `material`。                    |
| `offset`         | String  | `"0,0,0"` | 相对基准位置的平移，格式 `x,y,z`（米）。                                |
| `scale`          | String  | `"1,1,1"` | 缩放比例，可写 `sx,sy,sz` 或单值 `s` 表示统一缩放。                      |
| `rotation`       | String  | `"0,0,0"` | 欧拉角旋转，格式 `yaw,pitch,roll`，单位为度。                         |
| `center`         | Boolean | `true`    | 是否将展示对齐到方块中心。                                           |
| `viewRange`      | Float   | `32`      | 可视距离（米），超出后实体对玩家不可见。                                    |
| `billboard`      | String  | `"fixed"` | 展示面朝向模式：`fixed` / `center` / `vertical` / `horizontal`。 |
| `shadowRadius`   | Float   | `0`       | 阴影半径。                                                   |
| `shadowStrength` | Float   | `0`       | 阴影强度。                                                   |

---

## 🧠 参数格式示例

### ✅ 基础创建
```yaml
display-block {
  var = "cube"
  at = "@self"
  material = "EMERALD_BLOCK"
}
```

### ✅ 指定 BlockData 与初始状态
```yaml
display-block {
  var = "glass"
  blockdata = "minecraft:stained_glass[color=light_blue]"
  offset = "0,1.2,0"
  scale = "1.5"
  rotation = "45,0,0"
  billboard = "center"
}
```

### ✅ 与变量插值联动
```yaml
var { cubeVar = "bossCube" }
display-block {
  var = "${cubeVar}"
  at = "${target}"
  material = "OBSIDIAN"
  viewRange = "48"
}
```

---

## ⚡ 与控制语句搭配

生成的展示实体会被存储在 `ctx.vars[var]` 中，可交给 [`display-block-control`](display-block-control.md)
进行移动、旋转、缩放或动画。常见流程：

1. `display-block` 负责创建并保存实体。
2. 后续多次调用 `display-block-control` 以实现动画或状态变更。
3. 结合 `kether`、`if` 等语句可以实现条件展示与复杂逻辑。

---

## ⚠️ 注意事项

- 需要在 1.19+ 支持 `BlockDisplay` 的服务器环境使用。
- `blockdata` 字段必须符合服务器支持的 BlockData 语法，否则会抛出错误。
- 展示实体不会自动清理，如不再需要，请自行调用控制语句或其他逻辑移除。
- `viewRange` 过大可能导致客户端性能压力，建议根据场景合理设置。

---

## 🔗 相关语句

- [`display-block-control`](display-block-control—控制虚拟方块展示.md) — 控制 BlockDisplay
- [`display-item`](display-item—创建虚拟物品展示.md) — 创建 ItemDisplay
- [`display-item-control`](display-item-control—控制虚拟物品展示.md) — 控制 ItemDisplay